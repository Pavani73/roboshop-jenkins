- name: Create jenkins jobs
  hosts: localhost
  connection: local
  vars:
    jobs:
      - roboshop-terraform
      - cart
      - catalogue
      - shipping
      - user
      - payment
      - dispatch
      - frontend
  roles:
    - jenkins-jobs
